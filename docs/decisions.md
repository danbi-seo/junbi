# 결정 기록

판단이 갈린 지점을 **한 줄씩** 남긴다. 3개월 뒤에 "이거 왜 이렇게 했지" 하는 순간이 반드시 온다.

## 왜 파일로 두는가

이 프로젝트 결정의 상당수가 **무언가를 안 만든 것**이다. 부재는 코드에도 `git log`에도
흔적이 남지 않는다. 기록이 없으면 다음에 보는 사람(대개 몇 달 뒤의 본인)에게는
빠뜨린 것으로 보이고, 채워 넣는 순간 사고가 난다.

특히 이 넷은 **정책을 만드는 걸 잊은 것과 코드상 구분이 안 된다.**

| 부재 | 지우면 생기는 일 | 근거 |
|---|---|---|
| `profiles`에 성별 컬럼 없음 | 슬롯(a/b) 체계가 깨지고 카카오 검수에 묶임 | [19-health](19-health.md) B |
| `cycles`에 짝 조회 정책 없음 | 주기 원본(증상·통증·메모)이 상대에게 넘어감 | [19-health](19-health.md) F |
| `calendar_accounts` 정책 0개 | 구글 refresh token이 클라이언트에서 읽힘 | [18-calendar-sync](18-calendar-sync.md) |
| `notification_prefs`에 짝 조회 정책 없음 | 끄는 행위가 추궁 대상이 됨 | [13-notifications](13-notifications.md) |

## 쓰는 법

- 형식은 `YYYY-MM-DD  한 줄`. 이유가 필요하면 다음 줄에 들여쓰기로 붙인다
- **맨 아래에 덧붙인다.** 위를 고치지 않는다
- **뒤집힌 결정도 지우지 않는다.** 새 줄로 뒤집었다고 적는다.
  지우면 같은 고민을 처음부터 다시 하게 된다
- 문서를 고쳤으면 여기도 한 줄. 문서와 이 파일이 어긋나면 **이 파일이 최신**이다

---

## 2026-08-20 — 설계

```
2026-08-20  공개 수준 기본값 busy
2026-08-20  색·라벨을 성별이 아니라 슬롯(a/b)에
2026-08-20  위젯 요구로 Capacitor 검토 → 위젯 제외 결정으로 PWA 복귀
2026-08-20  위젯 대신 .ics 구독으로 기본 캘린더 앱 위젯을 빌림
2026-08-20  일정 임박 알림은 웹 푸시가 아니라 .ics VALARM(OS)이 담당
2026-08-20  .ics 발행을 로드맵 2단계로 앞당김. 핵심 가설을 일찍 검증
2026-08-20  주기 모듈을 성별 자동이 아니라 선택 활성화로
2026-08-20  건강 정보는 해제 시 유예 없이 즉시 파기
2026-08-20  공유 스위치를 끌 때 상대에게 알리지 않음
2026-08-20  상태 자동 위치 감지 안 함
2026-08-20  전화번호·성별 미수집. 번호를 받아도 본인 확인이 안 되고 유출 등급만 올라감
2026-08-20  카카오는 인증에만. 이름·생일은 앱에서 직접 입력받아 검수 정책에 안 묶이게
2026-08-20  페어링은 양쪽 확인(pending → active). 링크가 새어도 A가 막을 수 있게
2026-08-20  애칭에 '님' 고정. 조사 분기가 사라지고 이모지·영문 애칭도 동작
2026-08-20  성별 대신 온보딩 질문("주기를 기록할까요?"). 결과는 같고 수집이 줄어듦
2026-08-20  주기 공유는 스위치 하나. 상대에게는 시작·끝·임신가능 구간만
2026-08-20  임신 가능성이 높은 구간만 표시. 나머지 날은 무표시 (낮음/안전 라벨 금지)
2026-08-20  주기 관련 푸시 알림 없음. 잠금화면에 뜨는 건 압박이 됨
2026-08-20  여행 겹침 경고·지연 감지·알림 감소는 전부 본인 화면에만
2026-08-20  알림 설정을 수신/발신 두 축으로. 둘 다 켜져야 발송
2026-08-20  발신 설정은 상대에게 비공개. 공유 스위치와 같은 원칙
2026-08-20  제안·해제 알림은 설정으로 못 끔. 제안하는 행위가 곧 알리겠다는 의사
2026-08-20  루틴 상태를 테이블에 쓰지 않고 조회 시 계산. cron 실패 지점 제거
2026-08-20  자동 상태는 점선. 앱의 추정을 본인 말처럼 보이게 하지 않음
2026-08-20  메인을 상대 중심으로. 내 상태는 이미 알고 있으므로 우상단에 작게
2026-08-20  모바일 360px 기준으로 만들고 데스크톱으로 넓힘. 컴포넌트는 한 벌
2026-08-20  PC 경로를 버리지 않음. 구글 캘린더 구독이 PC 브라우저에서만 됨
2026-08-20  서비스명 JUNBI 확정
2026-08-20  코드 레포는 1단계 후 공개 검토. 백업 레포는 영구 Private
2026-08-20  NEXT_PUBLIC_ 접두어가 '공개해도 되는 것'의 기준
2026-08-20  Supabase·Vercel 모두 도쿄 리전. Supabase는 생성 후 변경 불가
            서울 대비 지연은 20~30ms 차이라 무시 가능. 둘을 맞추는 게 핵심
            보관 위치가 국외라 처리방침에 그대로 반영
2026-08-20  도메인은 선택. .vercel.app이면 완전 0원. 다만 .ics URL이 캘린더에 영구 저장됨
2026-08-20  Vercel 채택. 대역폭이 아니라 web-push가 Node 런타임을 요구해서
2026-08-20  백업 레포를 코드와 분리. 코드 공개 가능성과 권한 최소화
2026-08-20  pg_cron 주기 1분 → 5분. 월 43,200회 → 8,640회
2026-08-20  .ics에 ETag 필수. 폴링 횟수는 못 줄여도 폴링당 전송량은 줄일 수 있음
2026-08-20  .ics 범위를 미래 12개월 → 6개월로. 폴링마다 그 크기가 전송량이 됨
2026-08-20  위치 공유·사진 첨부 제외. 장소 첨부(링크→좌표)만 유지
            → 위치 권한을 한 번도 요청하지 않고, Storage를 안 쓰고, EXIF 유입 경로가 없음
```

---

## 2026-08-21 — 0단계 준비

```
2026-08-21  설계 문서 23개를 Downloads에서 docs/로 옮김. CLAUDE.md에서 참조 가능하게
2026-08-21  docs/01~22와 docs/README.md는 .gitignore로 커밋에서 제외
            예시 문구에 이름·애칭이 50여 곳 들어 있고, 히스토리에 한 번 들어가면
            나중에 파일을 고쳐도 git log -p에 남음. 1단계 후 공개 검토가 열려 있어
            처음부터 안 넣는 쪽을 택함. 유실 위험은 OneDrive 동기화로 덮음
            이 파일(decisions.md)만 예외로 커밋한다
2026-08-21  .env.example 커밋. 값이 아니라 키 이름 목록이 레포에 남는 게 목적
            NEXT_PUBLIC_ 경계가 diff에 드러나 서버 키 오접두어를 리뷰에서 잡을 수 있음
2026-08-21  결정 기록을 03-roadmap에서 decisions.md로 분리. 로드맵은 단계가 끝나면
            안 열게 되는 문서라 덧붙일 자리로 부적합
2026-08-21  애칭 표시를 partner_label(viewer, owner) SQL 함수 하나로 통일
            문서 곳곳의 profiles.nickname은 존재하지 않는 컬럼이었음
            auth.uid()를 쓰지 않아 service_role로 도는 .ics 라우트에서도 동작
2026-08-21  notification_queue.kind = notification_prefs 컬럼 접미사로 고정
            enqueue_partner_notification이 'send_'/'recv_' + kind로 컬럼을 찾으므로
            kind에 화이트리스트를 걸어 format %I 주입 경로를 막음
2026-08-21  set_status가 큐에 직접 넣던 걸 enqueue_partner_notification 위임으로
            발신 설정 검사가 빠져 있던 버그가 함께 사라짐
2026-08-21  ics 라우트가 couple_id를 profiles에서 조회. ics_tokens에 비정규화하지 않음
            해제로 profiles.couple_id가 null이 되면 자동으로 404가 됨
2026-08-21  events_touch 트리거를 마이그레이션에 명시. .ics ETag가 여기 걸려 있음
2026-08-21  settlement_balance를 profiles 기준으로 재작성
            per_expense를 group by 하면 한쪽만 결제한 초기 상태에서 잔액이 0
2026-08-21  본문 서체를 Pretendard 대신 Noto Sans KR로 시작
            Pretendard는 Google Fonts에 없어 폰트 파일 자체 호스팅이 필요하다
            next/font가 받아 심어주는 쪽이 오프라인(PWA)에도 유리
2026-08-21  세션 갱신을 proxy.ts에 둠. Next 16에서 middleware가 proxy로 개명
            설계서(Next 15 기준) 그대로 만들면 파일이 조용히 무시된다
2026-08-21  메일 링크 경로(/auth/callback)도 코드 로그인과 함께 유지
            새 계정의 첫 로그인은 Confirm signup 템플릿이 따로 나가므로
            템플릿을 하나만 바꾸면 코드가 아니라 링크가 온다
2026-08-21  비밀번호 로그인을 개발용 보조 경로로 추가
            무료 요금제 기본 메일은 시간당 2통이라 계정 2개 테스트에 바로 막힌다
            7단계에서 카카오가 붙으면 실사용자에게는 노출하지 않는다
2026-08-21  RLS 검증을 스크립트로 (scripts/verify-rls.mjs)
            설계서의 시나리오 17개가 전부 수동이었다. 손으로 하는 검사는 안 하게 된다
2026-08-21  마스킹 뷰를 security_invoker 대신 소유자 권한으로. 설계서 조합이 모순
            security_invoker=true 뷰는 원본 권한을 호출자 기준으로 검사하므로
            events의 select를 회수하면 뷰까지 막힌다. 둘을 동시에 만족할 수 없다
            RLS가 하던 행 필터를 뷰 안에 직접 넣어 해결. 원본은 계속 잠겨 있고
            행 필터와 열 마스킹이 한 파일에 모인다
            → 뷰를 고칠 때 where 절을 빠뜨리면 그대로 유출이다. 주석으로 명시
2026-08-21  테이블 GRANT를 마이그레이션에 명시
            Supabase 자동 부여는 특정 역할이 만든 테이블에만 적용되는데
            CLI 마이그레이션은 그 역할로 돌지 않아 로그인해도 42501이 났다
            RLS만 있고 GRANT가 없으면 아무도 못 읽는다 (반대로 GRANT만 있으면 전면 공개)
2026-08-21  도메인을 사지 않고 junbi.vercel.app으로 시작 (연 0원)
            나중에 도메인을 붙이면 두 사람 다 캘린더 구독을 지우고 다시 넣어야 한다
            확장하거나 호스팅을 옮길 때 재검토
2026-08-21  .ics와 Supabase Redirect URL에는 프로덕션 별칭만 쓴다
            junbi-<해시>-<팀>.vercel.app은 배포마다 바뀐다. 캘린더에 등록하면 깨진다
2026-08-21  로그인에서 shouldCreateUser=false
            배포 주소는 누구나 열 수 있어 모르는 사람이 계정을 만들고
            메일 할당량까지 소모할 수 있었다. 가입은 7단계 초대 링크로만
2026-08-21  백업 복원 테스트는 미결로 남김
            덤프 내용 검사(테이블 22개·정책 30개 존재)까지만 확인
            실제 복원은 Docker가 필요하다. 설치하면 supabase db diff도 같이 쓸 수 있다
```

---

## 2026-08-21 — 1단계

```
2026-08-21  Realtime을 events가 아니라 couple_signals(신호 전용 테이블)로
            events는 select 권한이 없어 Realtime이 아예 전달하지 못한다
            게다가 Realtime 페이로드에는 마스킹이 적용되지 않아,
            내용이 실려 오면 그대로 쓰고 싶은 유혹이 생긴다
            신호에는 "바뀌었다"는 사실과 시각뿐이라 샐 것이 구조적으로 없다
2026-08-21  events에 insert/update 후 .select()를 붙이지 않는다
            select 권한이 없어 return=representation이 403이 된다
            supabase-js는 기본이 return=minimal이라 그냥 두면 문제없다
2026-08-21  삭제는 소프트 삭제만. deleted_at을 채운다
            하드 삭제하면 max(updated_at)이 과거로 돌아가 .ics ETag가 어긋난다
2026-08-21  종일 일정은 끝을 다음 날 00:00으로 저장
            .ics의 DTEND 규칙과 같다. 하루 빼면 캘린더에 안 뜬다
```

---
## 2026-08-21 — 브랜드

```
2026-08-21  로고 확정 (손그림, 강아지+토끼, 짙은 갈색 선)
            중성색을 로고 선 색에 맞춰 따뜻하게 조정
            paper #F2F1EE → #FBF7F0 / ink #1A1823 → #3B2B22
2026-08-21  탭·홈 아이콘에는 글자를 뺀 캐릭터만 (public/mark.png)
            16px에서 JUNBI 글자는 안 읽힌다. 전체 로고는 로그인 화면처럼
            넓은 자리에서만 쓴다
2026-08-21  아이콘 배경을 투명이 아니라 크림색으로 채움
            투명 배경은 안드로이드 홈 화면에서 검게 나온다
2026-08-21  로고 배경(흰색)을 투명 처리. 가장자리에서 이어진 흰색만 제거
            전부 지우면 캐릭터 몸통까지 뚫린다. 선에 막혀 안 이어지는 흰색은 남긴다
2026-08-21  다크 모드용 로고 파일을 따로 둔다 (logo-dark.png / mark-dark.png)
            원본은 짙은 갈색 선이라 어두운 배경에서 그대로 묻힌다
            CSS filter: invert는 색상까지 뒤집혀 갈색이 청록으로 간다
            선을 크림색으로, 몸통을 투명으로 다시 그렸다
2026-08-21  슬롯 색은 자수정·황동 유지, 채도만 낮춤
            크림 배경에 맞춰 부드럽게. 적록색약 구분은 그대로 유지된다
            분홍/파랑으로 바꾸지 않는다 — 성별 코딩이 되고 확장 시 다 뜯어야 한다
```

---
## 2026-08-21 — 2단계

```
2026-08-21  핵심 가설 검증 — 구글 캘린더에서 .ics 구독이 실제로 동작
            "구독 절차가 번거로워 아무도 안 한다"면 위젯 대체 전략 자체가 실패였다
            그걸 4개월 뒤에 알면 늦어서 2단계로 앞당긴 항목이다
            남은 확인: 아이폰 기본 캘린더, 홈 화면 위젯 표시
2026-08-21  service_role에도 테이블 GRANT가 필요하다
            RLS는 건너뛰지만 테이블 권한은 건너뛰지 못한다
            .ics 라우트가 ics_tokens를 못 읽어 항상 404였다
            앞으로 만들 테이블에도 자동 적용되게 default privileges를 걸어 뒀다
2026-08-21  .ics의 시각을 UTC(Z)로 발행
            calendar에 timezone을 주면 ical-generator가 VTIMEZONE 없이
            시간대 없는 시각을 쓴다. 한쪽이 해외에 나가면 약속이 밀린다
            UTC는 절대 시각이라 어디서 봐도 같은 순간이다
2026-08-21  .ics에 내 비공개 일정은 포함한다
            내 캘린더이므로. 다만 주소가 새면 그대로 노출된다
            설정 화면에 공개 URL 경고를 눈에 띄게 두는 것으로 대응
2026-08-21  마스킹된 일정에는 VALARM(알림)을 걸지 않는다
            달력에서 제목을 가려도 알림이 시각을 알려주면 절반이 무의미하다
```

---
## 2026-08-21 — 2단계 마무리

```
2026-08-21  서비스 워커가 화면(HTML)과 API를 캐시하지 않는다
            설계서 예시는 API 아닌 GET을 전부 캐시하지만, 그러면 상대 일정이 담긴
            HTML이 기기에 남고 마스킹된 응답과 안 된 응답이 섞일 수 있다
            정적 자산만 캐시하고 오프라인일 때는 안내 화면만 보여준다
2026-08-21  화면들을 app/(app)/ 그룹으로 모아 공통 레이아웃 적용
            로그인 화면은 (auth) 그룹이라 하단 탭이 뜨지 않는다
2026-08-21  일정 임박 알림 기본값 1시간 전 → 10분 전
            설정 화면에서 바꿀 수 있게 함. 값을 바꾸면 .ics ETag를 밀어
            캘린더 앱이 새 알림을 받아가게 한다
```

---
## 2026-08-21 — 3단계

```
2026-08-21  VAPID 키 생성. 공개 키는 절대 바꾸지 않는다
            바꾸면 기존 푸시 구독이 전부 무효가 된다
            유출돼도 그대로 둔다 — 원래 브라우저에 나가는 값이다
2026-08-21  알림 판정을 큐에 넣는 시점에 한다. 발송 시점이 아니다
            발송 때 거르면 안 갈 알림이 쌓이고, 나중에 설정을 켜면
            과거 알림이 한꺼번에 터진다
2026-08-21  마스킹 규칙이 발신 설정보다 위다
            발신을 켜도 시간만·비공개 일정은 알림이 가지 않는다
            마스킹은 규칙이지 취향이 아니다
2026-08-21  일정 알림 트리거를 DB에 둔다 (서버 코드가 아니라)
            앱·SQL·나중의 캘린더 연동 어디서 일정이 생겨도 규칙이 같게 적용된다
2026-08-21  proxy에서 /api/* 전체를 세션 검사 예외로
            /api/ics는 비밀 토큰, /api/push는 CRON_SECRET으로 각자 인증한다
```

---
## 2026-08-21 — 4단계 (기념일)

```
2026-08-21  기념일 계산을 DB 함수가 아니라 TypeScript에 둠
            설계서는 upcoming_anniversaries() RPC를 제시하지만,
            음력 변환을 SQL에서 하려면 Edge Function으로 위임해야 한다
            TS 한 곳에 모으면 화면과 .ics 라우트가 같은 함수를 쓴다
2026-08-21  기념일 행을 미리 만들지 않고 조회 시 계산
            100일·200일·300일을 전부 행으로 만들면 수백 개가 쌓인다
2026-08-21  음력은 base_date에 양력으로 저장하고 is_lunar만 켠다
            음력 날짜를 그대로 저장하면 매번 "이게 음력인가"를 따져야 한다
            윤달 생일은 기본을 평달로. 없는 날짜는 그달 마지막 날로 물러난다
2026-08-21  기념일 알림은 -P7D와 -P1D만. 당일 0초 전은 걸지 않는다
            종일 일정의 0초 전은 자정에 울려서 자는 사람을 깨운다
2026-08-21  자동 생성 범위 — 1000일 이하는 100일 단위, 초과는 500일 단위
            10년 차 커플에게 3700일은 의미가 없지만 10주년은 의미가 있다
```

---
## 2026-08-21 — 4단계 (체크리스트)

```
2026-08-21  체크리스트는 Realtime 페이로드를 직접 쓴다
            일정과 달리 마스킹이 없어서 그대로 써도 샐 것이 없다
            일정은 신호만 받고 뷰에서 재조회한다 — 규칙이 반대다
2026-08-21  couple_signals 신호를 상태·기념일·체크리스트로 확대
            지금까지 일정만 올려서 상대가 상태를 바꿔도 화면이 안 바뀌었다
2026-08-21  체크는 낙관적 업데이트. 서버 응답을 기다리지 않는다
            마트 지하에서 신호가 끊기는 일이 잦고, 체크는 충돌해도 결과가 같다
2026-08-21  하단 탭을 5개로 유지 (달력·목록·기념일·루틴·설정)
            월 뷰는 메인 토글, 일정 추가는 ＋, 루틴은 상태 시트에서 들어간다
            장소·지출이 들어오면 기념일·루틴을 여기서 빼야 한다
2026-08-21  알림 큐와 지난 데이트 준비물을 매일 정리
            발송된 알림이 행으로 계속 남아 쌓이고 있었다
```

---
## 2026-08-21 — 4단계 (장소)

```
2026-08-21  지도를 카카오맵 SDK 대신 Leaflet + OpenStreetMap으로
            2026-07-21부터 카카오맵 무료 쿼터가 개발자 계정의 첫 앱에만 적용된다
            등록을 기다리지 않고 지금 동작하는 쪽을 골랐다 (설계서가 제시한 대안)
            지도 표시는 OSM, 장소 검색은 카카오 로컬 API로 나눈다
            검색은 KAKAO_REST_KEY가 없으면 그냥 안 켜진다 — 링크·직접 입력은 동작
2026-08-21  링크에서 좌표를 못 뽑으면 비워둔다. 자동 추측하지 않는다
            엉뚱한 좌표가 저장되는 게 빈칸보다 나쁘다
            좌표가 없으면 목록에만 뜨고 지도에는 안 찍힌다
2026-08-21  좌표를 한국 범위로 검사한다 (위 32~40, 경 124~132)
            위경도가 뒤바뀐 입력을 걸러낸다
2026-08-21  하단 탭에서 루틴을 빼고 장소를 넣음 (5개 유지)
            루틴은 내 상태 편집 시트에서 들어간다
            6단계에서 지출이 들어오면 기념일을 빼야 한다
```

---
## 2026-08-24 — 웹 푸시 실기기 확인

```
2026-08-24  아이폰(iOS 18)·크롬 양쪽에서 푸시 수신 확인. 3단계 핵심 검증 완료

2026-08-24  푸시 등록에서 막힌 지점 네 가지 (전부 "조용히 실패")
            1) push_subscriptions에 user_id를 안 넣어 저장이 거부됨
               NOT NULL이고 기본값이 없다. 결과를 확인 안 해 화면엔 "켜짐"이 떴다
            2) navigator.serviceWorker.ready는 등록된 워커가 없으면 영원히 기다린다
               개발 서버에서는 워커를 등록하지 않으므로 버튼이 멈춘다
            3) NEXT_PUBLIC_ 값은 빌드 때 코드에 새겨진다
               Vercel에 넣은 뒤 재배포하지 않으면 배포본에 안 들어간다
            4) subscribe() 거절을 .catch(()=>"timeout")으로 뭉개 원인을 버렸다

2026-08-24  실패 상태를 타입으로 나눈다 (save-failed · no-worker · timeout · error)
            "안 된다"만으로는 원인을 못 찾는다. 어느 단계에서 왜 막혔는지를 화면에

2026-08-24  기기 등록은 기기마다 따로다. 브라우저 권한만으로 판단하면 안 된다
            권한은 켜져 있는데 서버에 구독이 없는 상태가 실제로 생긴다
            설정 화면이 서버의 구독 유무를 함께 보고 재등록 버튼을 노출한다

2026-08-24  검증 명령을 이어붙일 때 실패가 묻히지 않게 한다
            grep이 못 찾아도 뒤 명령이 실행돼 "있음"으로 잘못 보고한 적이 세 번 있었다
            결과를 숫자나 ✓/✗로 명시 출력한다
```

---
## 2026-08-24 — 5단계 (빈 시간 찾기)

```
2026-08-24  빈 시간 찾기를 DB 함수로. 클라이언트 계산은 불가능하다
            정확히 계산하려면 상대의 비공개 일정 시간을 알아야 하는데,
            그 값은 클라이언트에 내려가면 안 된다. 둘이 양립하지 않는다
            security definer 안에서 계산하고 결과 구간만 내보낸다
2026-08-24  내 비공개는 후보로 남기고 경고만, 상대 비공개는 조용히 제거
            내 비공개는 취소되거나 조정 가능할 수 있다. 판단은 소유자가 한다
            상대 화면에는 그냥 그 시간이 없을 뿐이고 이유는 알 수 없다
2026-08-24  제안 알림은 propose_slot이 보내고 events 트리거는 건너뛴다
            둘 다 보내서 한 제안에 알림이 두 개 갔다
            수락으로 confirmed가 될 때도 트리거를 막는다
2026-08-24  결과에 일정 제목·소유자를 넣지 않는다. 구간과 점수만
            "무엇 때문에 막혔는지"가 새면 마스킹이 무의미해진다
2026-08-24  후보는 상위 5개만. 10개를 주면 아무것도 고르지 못한다
```

---
## 2026-08-24 — 5단계 마무리

```
2026-08-24  구글 캘린더 연동을 만들지 않는다
            두 사람 다 구글 캘린더를 쓰지 않는다. 일정을 JUNBI에 직접 넣는다
            연동의 목적은 "빈 시간 찾기를 정확하게" 하나였는데,
            외부에 일정이 없으면 그 목적 자체가 없다
            OAuth 등록 비용만 남으므로 만들지 않는다
            → 나중에 필요해지면 18-calendar-sync.md를 그대로 따르면 된다
            → 함께 보류: .ics 구독 가져오기, 캘린더 선택 화면, 제목 미저장 옵션
2026-08-24  이로써 5단계는 빈 시간 찾기 + 제안으로 완료
```

---
## 2026-08-25 — 6단계 지출 · 정산

```
2026-08-25  범위를 계산기 + 기록장으로 못박는다
            안 함: 계좌·카드 연동, 실제 송금, 영수증 OCR, 월별 예산 대시보드
            돈이 앱 안에서 움직이는 순간 금융 규제와 분쟁 처리가 따라온다
            필요한 건 "누가 얼마 더 냈나" 하나다
2026-08-25  잔액은 DB 함수 settlement_balance() 한 곳에서만 계산한다
            화면에서 또 더하면 언젠가 두 숫자가 갈리고, 그게 돈이면 싸움이 된다
2026-08-25  잔액을 부호(+12,400)가 아니라 문장으로 보여준다
            "OO님에게 12,400원 받을 차례예요" — 부호는 방향이 순간적으로 헷갈린다
2026-08-25  홀수 반반은 결제자가 더 내는 쪽으로 버린다
            33,333원 → 상대 16,666원 · 결제자 16,667원
            반대로 하면 "네가 1원 더 냈네"가 된다. 결제한 쪽이 손해 보는 방향이 안전
2026-08-25  한쪽만 결제한 초기 상태에서 잔액이 0으로 나오던 버그를 막았다
            per_expense를 group by 하면 행이 하나뿐이라 desc와 asc가 같은 행을 집는다
            profiles를 기준으로 두 사람 모두에 대해 행을 만든다
2026-08-25  정산은 한쪽이 누르면 즉시 반영. 상대 승인을 요구하지 않는다
            승인 대기를 만들면 "왜 승인 안 해?"라는 새 마찰이 생긴다
            잘못 눌렀으면 이력에서 되돌린다
2026-08-25  지출 알림은 15분 창으로 묶는다
            데이트 하루에 지출이 다섯 번 생긴다. 그때마다 울리면 알림을 꺼 버린다
            같은 창에 또 들어오면 이전 예약을 지우고 합계를 다시 넣는다
2026-08-25  지출에도 건별 🔕를 둔다
            금액은 앱에서 어차피 보이니 알림을 안 보내도 정보가 사라지지 않는다
2026-08-25  하단 탭에서 D-day를 빼고 '쓴 돈'을 넣는다 (탭 최대 5개)
            D-day는 메인 달력 아래 띠로 항상 떠 있어 한 번에 닿는다
            기념일이 없을 때도 띠를 띄운다 — 그 띠가 유일한 입구다
```

---
## 2026-08-25 — 6단계 컨디션 · 주기

```
2026-08-25  "생리 주기는 여자쪽에서만"을 성별 컬럼이 아니라 본인 스위치로 구현
            결과는 요청과 같다 — 둘 중 한 사람만 기록 화면이 생긴다
            판단 기준만 다르다: 앱이 성별로 나누지 않고 본인이 켠다
            profiles에 성별 컬럼 없음은 되돌릴 수 없는 고정값이고,
            성별은 민감정보라 받는 순간 검수 항목이 늘고
            "수집하지 않은 정보는 유출될 수 없다"가 깨진다
            → health_sharing.cycle_module_on 하나가 성별 컬럼을 대신한다
2026-08-25  health_sharing의 '짝 공개설정 조회' 정책을 지웠다
            상대가 share_cycle = false를 볼 수 있으면 끄는 행위가 추궁 대상이 된다
            notification_prefs에 짝 조회 정책을 두지 않은 것과 같은 이유다
            앱은 이 테이블을 직접 읽지 않는다 — partner_health()가 안에서 읽고
            꺼져 있으면 아무것도 내보내지 않는다
            '꺼져 있음'과 '기록이 없음'이 상대 화면에서 구별되면 안 된다
2026-08-25  cycle_prediction(uuid)의 execute를 authenticated에서 회수했다
            인자로 남의 uuid를 받는 함수를 열어두면 상대가 직접 불러서
            주기 길이·편차·최근 간격을 그대로 가져간다. 내부에서만 부른다
            predicted_period_range(uuid)도 같다
2026-08-25  건강 정보에 Realtime을 붙이지 않는다 (Live 컴포넌트 미사용)
            Realtime 페이로드에는 마스킹이 적용되지 않아 원본이 그대로 나간다
            앱 진입 시 갱신으로 충분하다. 실시간일 필요가 전혀 없다
2026-08-25  배란은 다음 예정일 − 14로 거꾸로 센다
            황체기는 개인차가 적지만 난포기는 사람마다 달마다 크게 변한다
            마지막 시작일 + 14로 앞으로 세면 틀린다
            다만 다음 예정일 자체가 추정치라 추정 위에 쌓은 추정이다
            → 구간을 넓게 (next−19 ~ next−11)
2026-08-25  예측을 안 하는 조건을 둘 둔다 — 3회 미만, 편차 7일 이상
            틀린 예측을 주는 것보다 없는 게 낫다
            평균이 아니라 중앙값을 쓴다. 한 번 어긋난 주기가 전체를 끌고 간다
2026-08-25  빈 시간 찾기에서 예상 기간은 점수만 −25, 제외하지 않는다
            그 기간에 만나면 안 되는 게 아니다
            점수 계산에서 누구 것인지 구별하지 않는다 —
            구별하면 점수 차이로 상대의 주기를 역산할 수 있다
            이유(⚠️ 생리 예상 기간)는 본인 화면에만 뜬다
2026-08-25  모듈을 끄거나 동의를 철회하면 기록이 즉시 파기된다. 유예 없음
            헤어진 뒤에도 상대의 주기 데이터가 서버에 남아 있다는 사실의 무게가
            재결합 가능성보다 크다
            → 파기 전에 JSON·CSV로 내보낼 수 있게 했다
2026-08-25  주기 관련 푸시 알림을 만들지 않는다
            잠금화면에 뜨고 매달 특정 날짜에 알림이 오는 건 그 자체로 압박이다
            지연 감지도 본인에게만, 푸시가 아니라 앱 안 배너로만
2026-08-25  컨디션을 '내 몸' 한 곳으로 모은다. 상태 칩에서 뺐다
            이름이 겹치는 게 표면이고, 진짜 문제는 상대에게 나가는 경로가
            둘이었다는 것이다. 그런데 상태 칩에는 공개 스위치가 없어서
            share_condition을 꺼도 칩으로는 그대로 나갔다
            끄는 스위치를 우회하는 경로가 남아 있으면 스위치가 있으나 마나다
            화면에서 프리셋만 빼는 건 '막은 게 아니다' —
            set_status가 kind=condition을 직접 거부한다 (CONDITION_MOVED)
            status_kind enum의 condition은 남긴다. 값 제거는 되돌리기 어렵고
            남은 행은 전부 만료된 것뿐이다
2026-08-25  버그: 상태 '30분'이 1시간으로 저장되고 있었다
            set_status의 p_hours가 int라 0.5가 1로 반올림되고,
            greatest(p_hours, 1)이 한 번 더 밀어 올렸다
            서버 액션에도 Math.max(1, Math.round(hours))가 있었다
            numeric으로 바꾸고 옛 오버로드를 명시적으로 지웠다 —
            안 지우면 PostgREST가 어느 함수인지 못 고른다
2026-08-25  내 상태 편집을 흐름 안이 아니라 떠 있는 패널로
            문서 흐름에 두니 열 때마다 아래 내용이 통째로 밀려 내려갔다
            상태를 바꾸려다 상대 상태와 달력을 놓치게 된다
            바깥 누르기·Esc로 닫힌다. 하단 탭(z-10) 위에 온다
2026-08-25  달력 칸에 시작·끝을 글자로 적는다
            구간을 한 색으로만 칠하면 어디서 시작해 어디서 끝났는지 안 보인다
            날짜를 잘못 눌렀을 때 알아채는 게 이 화면의 핵심이다
            끝을 안 눌렀으면 마지막 날은 추정이라 '끝'으로 적지 않는다
2026-08-25  이미 기록된 날은 그 버튼을 비활성으로 둔다
            시작을 다시 누르면 3일 규칙에 걸려 시작일이 조용히 옮겨지고,
            끝을 다시 누르면 같은 날로 덮어써진다
            둘 다 아무 일도 안 일어난 것처럼 보이는데 실제로는 기록이 바뀐다
2026-08-25  기록 안의 날짜를 시작으로 누르면 새로 만들지 않고 시작일을 옮긴다
            3일 규칙 밖(5일 어긋남)이면 겹치는 기록이 생겼다
            겹치면 간격이 5일로 잡혀 15~60일 필터에 버려지고 목록만 엉망이 된다
            시작이 끝을 넘어가면 끝을 비운다 —
            제약에 걸려 저장이 통째로 실패하면 고칠 방법이 없어진다
2026-08-25  검증 스크립트의 쓰기를 A 계정으로 한정했다
            B가 실사용 계정이 되면서 전체 삭제가 실제 기록을 지울 수 있게 됐다
            '짝이 못 읽는다' 검사도 건수가 아니라 A의 기록이 안 나오는지로 바꿨다
            (B가 자기 기록을 읽는 건 정상인데 건수로 보면 실패로 잡혔다)
2026-08-25  종료를 안 눌렀을 때의 기간을 본인 기록 중앙값으로 (period_duration)
            네 군데에 5일로 박아 뒀던 게 버그였다. 6~7일인 사람은
            상대 화면의 '🩸 생리 중'이 이틀 먼저 꺼지고,
            컨디션 화면의 주기 항목도 이틀 먼저 사라지고,
            달력 기록 구간도 짧게 칠해졌다
            예측의 nextDuration은 이미 중앙값이었는데 나머지가 안 따라갔다
            기록이 없을 때만 5일 (일반적인 범위 3~7일의 가운데)
            periodDuration은 상대에게 나가지 않는다 — 며칠짜리인지는
            상대가 알 것이 아니다
2026-08-25  임신 가능성 구간을 무채색에서 분홍으로 바꿨다 (--fertile)
            문서는 무채색을 권했지만 실제로 눈에 안 들어와 표시한 의미가 없었다
            빨강은 안 쓴다 — --danger가 이미 빨강이라 삭제·오류와 같은 색이 되고
            임신 가능성은 경고가 아니다
            슬롯 색(a/b)도 안 쓴다 — 사람을 가리키는 색이라 의미가 섞인다
            채우기는 옅게, 글자와 테두리로 존재감을 낸다
            꽉 찬 색은 '기록'만 쓴다. 추정치가 확정과 같은 무게로 보이면 안 된다
            ▒ 기호와 disclaimer는 그대로 둔다 (색으로만 구분하지 않는다)
2026-08-25  달력 칸의 선택·오늘 표시를 ring에서 outline으로 바꿨다
            임신 가능성 칸이 ring을 쓰게 되면서 같은 속성을 다투게 됐다
2026-08-25  달력에서 지난 날짜를 눌러 소급 입력한다
            시작일을 놓치는 일이 잦은데, 큰 버튼 하나만 두면 오늘밖에 못 넣는다
            그 주기가 통째로 빠지면 간격이 두 배가 되고 평균이 무너진다
            앞으로의 날짜는 눌리지 않는다 (서버도 FUTURE_DATE로 막는다)
2026-08-25  하루 차이로 다시 시작을 누르면 새 기록을 만들지 않고 날짜를 옮긴다
            3일 안쪽은 오타로 본다. 이틀짜리 유령 주기가 생기면 예측이 무너진다
2026-08-25  종료는 그 날짜 이전에 시작한 것 중 가장 최근 하나만 닫는다
            소급 입력이 붙으면서 열린 기록이 둘이 될 수 있게 됐다
            전부 닫으면 오늘 '끝났어요'가 지난달 기록까지 닫는다
2026-08-25  달력에 뜨는 건 두 가지다 — 생리 주기 · 임신 가능성 높음
            셋으로 늘어놓으면 무엇이 사실이고 무엇이 추정인지가 흐려진다
            다만 생리 주기 안에서 기록(진하게)과 예상(연하게)은 구분한다
            추정치가 확정된 기록과 똑같이 보이면 과신한다 — 실선/점선 규칙
2026-08-25  컨디션 기록은 남긴다 (기운·아픈 곳)
            기운이 낮은 날 상대에게 가는 알림을 줄이는 동작이 여기 물려 있다
2026-08-25  '쓴 돈'을 'our Pay'로 바꾼다
            our Day · our Places · our List와 같은 결. 탭에서 바로 읽힌다
2026-08-25  버그: my_health()가 conditions·cycles 조인에서 memo를 수식하지 않아
            42702로 통째로 실패했다. set_health_sharing()이 끝에서 그걸 부르는
            바람에 '모듈 끄기 → 즉시 파기'까지 같이 롤백됐다
            → 조인을 없애고 각각 따로 읽는다 (0026)
            stable 함수 안의 insert도 같이 제거했다
```

---
## 미결 — 정해야 하는 것

결정하면 위 로그로 옮긴다.

```
[ ] .ics 구독자에게 푸시 임박 알림을 끌지
    ics_tokens.last_read가 차 있으면 recv_event_upcoming을 자동으로 내릴지,
    아니면 사용자가 직접 끄게 둘지. 안 정하면 같은 알림이 두 번 온다 → 13-notifications
[ ] 코드 레포를 공개할지            1단계 후 재검토. 백업 레포는 영구 Private
```
